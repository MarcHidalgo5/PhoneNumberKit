skip:
  mode: native

build:
  contents:
    - block: "android"
      contents:
        - block: "sourceSets"
          contents:
            - block: 'getByName("main")'
              contents:
                - 'assets.srcDir(layout.projectDirectory.dir("src/main/swift/${project.name}/Resources"))'
